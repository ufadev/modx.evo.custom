/**
 * sitemap_changefreq
 *
 * Для карты сайта
 *
 * @category        tv
 * @name            sitemap_changefreq
 * @internal        @caption Период обновления 
 * @internal        @input_type dropdown
 * @internal        @input_options Постоянно==always||Каждый час==hourly||Ежедневно==daily||Еженедельно==weekly||Ежемесячно==monthly||Ежегодно==yearly||Никогда==never
 * @internal        @input_default weekly
 * @internal        @output_widget 
 * @internal        @output_widget_params 
 * @internal        @lock_tv 0
 * @internal        @template_assignments Home
 * @internal        @modx_category 9. SEO
 * @internal        @installset sample
 */
